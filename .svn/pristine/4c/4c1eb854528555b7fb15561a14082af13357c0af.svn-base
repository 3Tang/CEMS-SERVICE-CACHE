package com.vrv.cems.service.cache;

import org.apache.log4j.Logger;
import org.apache.thrift.TException;
import org.junit.Before;
import org.junit.Test;

import com.vrv.cems.service.base.bean.cache.DeviceCache;
import com.vrv.cems.service.base.bean.cache.DeviceKeyCache;
import com.vrv.cems.service.base.bean.cache.Result;
import com.vrv.cems.service.base.interfaces.ICacheService;
import com.vrv.cems.service.cache.action.ICacheServiceAction;

public class Account2UserOnlyIdTest {
	/*private Logger log = Logger.getLogger(Account2UserOnlyIdTest.class);
	String account;
	String userOnlyId;
	private ICacheServiceAction iCacheServiceAction=new ICacheServiceAction();
	private Result result;
	@Before
	public void init(){
		
		account="13718705181";
		userOnlyId="a5019b56795b49fe8ce1cd0d3477efd6";
		
	}
	
	
	//验证Key在缓存中是否已存在
	@Test
	public void operation() throws TException
	{
		result = iCacheServiceAction.saveAccount2UserOnlyId(ICacheService.SERVICE_CODE, ICacheService.SAVEACCOUNT2USERONLYID, account,userOnlyId);
		log.info("第一步存储"+result.getCode()+":"+result.getInfo());
		
		//String userOnlyId="77777777777777777777777777777777";
		
		result=iCacheServiceAction.isExist(ICacheService.SERVICE_CODE, ICacheService.ISEXIST,ICacheService.PREFIX_ACCOUNT,account);
		log.info("第二步判断是否存在"+result.getCode()+":"+result.getInfo());
		
		String userOnlyId=iCacheServiceAction.queryAccount2UserOnlyId(ICacheService.SERVICE_CODE, ICacheService.QUERYACCOUNT2USERONLYID,account);
		log.info("第三步查询"+"userOnlyId:"+userOnlyId);
		
		result = iCacheServiceAction.deleteAccount2UserOnlyId(ICacheService.SERVICE_CODE, ICacheService.DELETEACCOUNT2USERONLYID,account);
		log.info("第四步删除"+result.getCode()+":"+result.getInfo());
		
		result = iCacheServiceAction.isExist(ICacheService.SERVICE_CODE, ICacheService.ISEXIST,ICacheService.PREFIX_ACCOUNT,account);
		log.info("第五步再次判断是否存在"+result.getCode()+":"+result.getInfo());
	}
	
	
	*/
}
